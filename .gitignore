data/ecolibrium_directory.db
data/commonweave_directory.db
data/*.log
data/paperclip-fix/
data/clear_sessions*.mjs
data/fix_stuck_run*.mjs
data/inspect_db.mjs
data/researcher-*.log
data/aggregator-*.log
data/eco1_report.json
data/DIRECTORY_P2.md
data/regional/DIRECTORY_P2.md

# Large audit/backup files
data/trim_audit/
data/*.db.backup-*
data/*.db.backup

# Large per-source CSV caches. The .txt caches from older ingesters
# (grounded-solutions, habitat, ituc) are tiny and stay tracked. The CSVs
# pulled by the Wave A ingesters (Brazil ~324 MB, Australia ~15 MB) are
# regenerated by --refresh and do not belong in git.
data/sources/acnc-cache/
data/sources/mapa-oscs-cache/
data/sources/bulgaria-npo-cache/

# DeepSeek critique bundle (local only)
.deepseek-bundle.md
_scratch/

# Node
node_modules/
**/node_modules/
npm-debug.log*

# Local civic-identity/neighborhood-os runtime artifacts
civic-identity.db
neighborhood-os.db
*.db-wal
*.db-shm
*.db.lock
smoke-test.db*
fed-a.db*
fed-b.db*

# Per-node operator config (the example file is in the repo; real one is local)
node.config.json